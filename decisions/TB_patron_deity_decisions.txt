#NOT for picking actual patrons, instead here are the decisions having a patron unlocks!

decisions = {
	# Tyr
	
	tyr_maim_self = {
		ai_check_interval = 30
		potential = {
			trait = patron_tyr
			OR = {
				AND = {
					has_dlc = "Reaper's Due"
					NOT = { trait = one_handed }
				}
				AND = {
					NOT = { has_dlc = "Reaper's Due" }
					NOT = { trait = maimed }
				}
			}
		}
		allow = {
			trait = zealous
			NOT = { trait = craven }
		}
		effect = {
			character_event = { id = tyrevents.1 }
		}
		ai_will_do = {
			factor = 5
			modifier = {
				factor = 10
				trait = lunatic
			}
			modifier = {
				factor = 1.5
				trait = brave
			}
		}
	}
	
	tyr_blind_self = {
		ai_check_interval = 30
		potential = {
			trait = patron_tyr
			NOT = { trait = blinded }
			year = 1358
		}
		allow = {
			trait = zealous
			NOT = { trait = craven }
		}
		effect = {
			character_event = { id = tyrevents.2 }
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 10
				trait = lunatic
			}
			modifier = {
				factor = 1.5
				trait = brave
			}
		}
	}
}