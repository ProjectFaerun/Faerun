decisions = {

	check_mr_incumbency = {
		ai = no
		potential = {
			is_patrician = yes
			not_casting = yes
		}
		allow = {
			always = yes
		}
		effect = {
			if = {
				limit = { is_merchant_republic = yes }			
				character_event = { id = frelections.8 } 
			}
			else = {			
				character_event = { id = frelections.12 } 
			}			
		}
		ai_will_do = {
			factor = 0
		}
	}
}
