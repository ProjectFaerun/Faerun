decisions = {
	
	fighter_duel = {
		is_high_prio = yes
		ai_check_interval = 4
		
		potential = {
			always = no # unfinished!
			OR = {
				trait = trained_fighter
				trait = journeyman_fighter
				trait = expert_fighter
				trait = master_fighter
				trait = renowned_fighter
				trait = legendary_fighter
			}
		}
		allow = {
			NOT = { has_severe_disability_trigger = yes }
			custom_tooltip = {
				text = FIGHTDUELCD
				hidden_tooltip = {
					OR = {
						NOT = { has_character_flag = fightduelcd }
						had_character_flag = 3
					}
				}
			}
			in_battle = yes
			custom_tooltip = {
				text = FIGHTDUELENEMYALIVE
				hidden_tooltip = {
					enemy = {
						is_alive = yes
					}
				}
			}
		}
		effect = {
			
		}
		ai_will_do = {
			factor = 0.5
		}
	}
	
}