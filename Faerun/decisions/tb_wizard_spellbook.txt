

targeted_decisions = {
	
	cantrip_friends = { # This will be a player-only spell because the AI can't really deal with the strategy
		filter = realm
		only_playable = yes
		
		from_potential = {
			z_wizard = yes
		}
		
		potential = {
			ai = no
		}
		
		allow = {
			custom_tooltip = {
				text = CANTRIP_ON_COOLDOWN
				hidden_tooltip = {
					NOT = {
						had_character_flag = {
							flag = spell_friends_cooldown
							months = 1
						}
					}
				}
			}
			NOT = {
				has_opinion_modifier = {
					who = FROM
					name = friends_spell
				}
			}
			NOT = {
				has_opinion_modifier = {
					who = FROM
					name = friends_spell_after
				}
			}
		}
		
		effect = {
			spell_resistance_check = yes
			opinion = {
				modifier = friends_spell
				who = FROM
				months = 2
			}
			repeat_event = { id = spellaux.2 days = 60 }
		}
	}
	
}