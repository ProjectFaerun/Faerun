decisions = {
  allow_pillaging = {
    only_playable = yes
    is_high_prio = yes

    potential = {
      OR = {
        independent = yes
        ai = yes
      }
      not_casting = yes
      has_character_flag = no_pillaging
    }

    allow = {
      independent = yes
      has_character_flag = no_pillaging
    }

    effect = {
      clr_character_flag = no_pillaging
    }

    ai_will_do = {
      factor = 1
      modifier = {
        factor = 0
        OR = {
          trait = charitable
          trait = temperate
          trait = kind
          trait = just
          trait = trusting
        }
      }
    }
  }

  forbid_pillaging = {
    only_playable = yes
    is_high_prio = yes

    potential = {
      OR = {
        independent = yes
        ai = yes
      }
      not_casting = yes
      NOT = { has_character_flag = no_pillaging }
    }

    allow = {
      independent = yes
      NOT = { has_character_flag = no_pillaging }
    }

    effect = {
      set_character_flag = no_pillaging
    }

    ai_will_do = {
      factor = 1
      modifier = {
        factor = 0
        NOR = {
          trait = charitable
          trait = temperate
          trait = kind
          trait = just
          trait = trusting
        }
      }
    }
  }
}
