namespace = absent

character_event = {
	id = absent.1
	hide_window = yes
	
	is_triggered_only = yes
	
#	trigger = {
#		trait = absent
#	}
	
	immediate = {
		any_character = {
			limit = {
				trait = absent
			}
		}
		death = { death_reason = death_missing }
	}
}