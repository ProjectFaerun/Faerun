namespace = PDE

character_event = {
  id = PDE.1
  hide_window = yes

  is_triggered_only = yes

  trigger = {
    dynasty = 12851
    FROM = {
      location = {
        county = {
          title = c_waterdeep
          NOT = { has_title_flag = dragged_into_abyss }
        }
      }
    }
  }

  immediate = {
    FROM = {
      location = {
        wipe_pops = yes
        culture = baatezu
        religion = infernal
        county = {
          set_title_flag = dragged_into_abyss
        }
      }
    }
  }
}

character_event = {
  id = PDE.2
  hide_window = yes

  is_triggered_only = yes

  trigger = {
    has_character_flag = i_am_telamont
    FROM = {
      location = {
        county = {
          title = c_shoal_of_thirst
        }
      }
    }
  }

  immediate = {
    FROM = {
      location = {
        wipe_pops = yes
        add_main_race_pop = yes
      }
    }
  }
}
