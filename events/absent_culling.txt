namespace = absent

character_event = {
	id = absent.1
	hide_window = yes
	
	is_triggered_only = yes
	
	trigger = {
		trait = absent
	}
	
	immediate = {
		death = { death_reason = death_missing }
	}
}